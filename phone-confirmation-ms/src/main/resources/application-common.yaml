server:
  port: ${API_SERVER_PORT:8081}
  servlet:
    application-display-name: phone-confirmation-ms
spring:
  application:
    name: phone-confirmation-ms
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka