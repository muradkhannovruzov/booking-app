server:
  port: ${API_SERVER_PORT:8081}
  servlet:
    application-display-name: provider-ms
spring:
  application:
    name: provider-ms
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka